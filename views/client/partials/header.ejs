<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Your Page Title</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    .category-bar {
      display: grid;
      grid-template-columns: repeat(9, 1fr);
      /* 9 equal parts */
      gap: 1rem;
      text-align: center;
    }

    .category-item:hover .subcategory-menu {
      display: block;
    }

    .subcategory-menu {
      display: none;
      position: absolute;
      left: 0;
      top: 100%;
      background-color: #fff;
      padding: 10px;
      z-index: 10;
      min-width: 200px;
      max-width: 300px;
      max-height: 300px;
      overflow-y: auto;
      white-space: nowrap;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }


    .dropdown-subitem,
    .dropdown-extraitem {
      padding: 4px 8px;
      font-size: 14px;
    }


    .dropdown-subitem:hover .extra-dropdown {
      display: block;
    }

    .extra-dropdown {
      display: none;
      position: absolute;
      top: 0;
      left: 100%;
      /* default */
      background-color: #fff;
      min-width: 180px;
      max-width: 240px;
      max-height: 300px;
      overflow-y: auto;
      white-space: nowrap;
      padding: 10px;
      z-index: 20;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    }


    /* If near the edge, open to the left */
    .category-item:last-child .extra-dropdown {
      left: auto;
      right: 100%;
    }


    .dropdown-extraitem {
      padding: 4px 8px;
      white-space: nowrap;
    }

    .banner-container {
      overflow-x: auto;
      white-space: nowrap;
    }

    .banner-item {
      display: inline-block;
      margin-right: 10px;
    }

    .banner-item img {
      height: 170px;
      width: 280px;
      object-fit: cover;
      border-radius: 8px;
    }

    .five-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .five-grid .card {
    height: 100%;
    text-align: center;
  }

  @media (max-width: 992px) {
    .five-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 576px) {
    .five-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  </style>

</head>

<body class="container">

  <!-- Header -->
  <header class="bg-white py-2 shadow-sm border-bottom">
    <div class="container d-flex align-items-center justify-content-between">

      <!-- Logo -->
      <a href="/" class="text-decoration-none d-flex flex-column me-3">
        <span class="fw-bold text-primary fs-5">Flipkart</span>
        <small class="text-muted" style="margin-top: -4px;">
          Explore <span class="text-warning fw-bold">Plus ✨</span>
        </small>
      </a>

      <!-- Search Bar -->
      <form class="flex-grow-1 mx-3">
        <div class="input-group">
          <span class="input-group-text bg-light border-end-0">
            <i class="bi bi-search"></i>
          </span>
          <input type="text" class="form-control border-start-0" placeholder="Search for Products, Brands and More">
        </div>
      </form>

      <!-- Right Icons -->
      <div class="d-flex align-items-center gap-4">

        <!-- Login Dropdown -->
        <div class="dropdown">
          <a href="#" class="text-dark text-decoration-none dropdown-toggle d-flex align-items-center"
            data-bs-toggle="dropdown">
            <i class="bi bi-person fs-5 me-1"></i> Login
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="#">My Profile</a></li>
            <li><a class="dropdown-item" href="#">Orders</a></li>
            <li><a class="dropdown-item" href="#">Wishlist</a></li>
          </ul>
        </div>

        <!-- Cart -->
        <a href="#" class="text-dark text-decoration-none d-flex align-items-center">
          <i class="bi bi-cart fs-5 me-1"></i> Cart
        </a>

        <!-- Become a Seller -->
        <a href="#" class="text-dark text-decoration-none d-flex align-items-center">
          <i class="bi bi-shop fs-5 me-1"></i> Become a Seller
        </a>

        <!-- More Icon -->
        <a href="#" class="text-dark">
          <i class="bi bi-three-dots-vertical fs-5"></i>
        </a>
      </div>
    </div>
  </header>
